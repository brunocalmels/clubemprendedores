<!-- <p id="notice"><%= notice %></p> -->

<h1>Reservas</h1>

<table>
  <thead>
    <tr>
      <th>Comienza</th>
      <th>Termina</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reservas.each do |reserva| %>
      <tr>
        <td><%= reserva.start_time %></td>
        <td><%= reserva.end_time %></td>
        <td><%= link_to 'Ver', reserva %></td>
        <td><%= link_to 'Editar', edit_reserva_path(reserva) %></td>
        <td><%= link_to 'Borrar', reserva, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva reserva', new_reserva_path %>
