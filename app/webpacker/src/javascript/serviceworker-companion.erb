console.log("[SW Companion] Invoking!");
if (navigator.serviceWorker) {
    navigator.serviceWorker
        .register('assets/packs/javascript/serviceworker', {
            scope: "./"
        })
        .then(function (reg) {
            console.log("[Page] Service worker registered!");
        });
}